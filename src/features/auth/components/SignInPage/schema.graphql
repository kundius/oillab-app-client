mutation AuthSignInPage ($input: SignInInput!) {
  signIn(input: $input) {
    success
    token
    record {
      id
    }
    error {
      ... on Error {
        message
      }
      ... on AuthValidationError {
        field
        message
      }
    }
  }
}
