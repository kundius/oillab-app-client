mutation VehicleCreatePage($input: VehicleCreateInput!) {
  vehicleCreate(input: $input) {
    success
    error {
      ... on Error {
        message
      }
    }
    record {
      id
    }
  }
}
