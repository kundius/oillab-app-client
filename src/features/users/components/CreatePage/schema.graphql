mutation UsersCreatePage($input: UserCreateInput!) {
  userCreate(input: $input) {
    success
    error {
      ... on Error {
        message
      }
    }
    record {
      id
      name
      email
    }
  }
}
